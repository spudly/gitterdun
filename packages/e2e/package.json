{
  "name": "@gitterdun/e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "pretest": "node ./scripts/ensure-ports-free.mjs",
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "test:ui": "PLAYWRIGHT_SKIP_WEBSERVER=1 playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "install": "playwright install",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^22.10.7",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@gitterdun/api": "file:../api",
    "@gitterdun/web": "file:../web"
  }
}
