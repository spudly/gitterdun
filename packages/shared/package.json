{
  "name": "@gitterdun/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "--prebuild": "rm -rf dist",
    "build": "tsc",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "jest",
    "test:dev": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
