{
  "name": "@gitterdun/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "--prebuild": "rm -rf dist",
    "type-check": "tsc",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint --max-warnings 0 src/**/*.ts",
    "test": "jest",
    "test:dev": "jest --watch"
  },
  "dependencies": {
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@jest/globals": "^30.0.5",
    "typescript": "^5.6.3"
  }
}
